# 开发与发布约定

## 沟通与范围

- 区分已确认事实、推测和未验证信息；不得用源码检查通过代替实际交互验证。
- 用户要求分析时，先定位并说明原因，不擅自修改应用或发布。
- 用户要求修改应用代码时，默认包含发布 IPA；只有确认 Release 的 IPA 资产已上传且可下载，才报告发布完成。
- 仅修改开发文档、约定或说明文件，不生成 IPA、不递增应用版本。
- 保留用户及其他任务的未提交修改，只提交本次任务范围内的文件。

## 验证与构建

- 日常 IPA 发布以必要检查、真机目标编译归档、打包、上传为基本流程。真机目标编译是生成 IPA 的必要步骤，不等于真机验收。
- 模拟器测试按本次修改和风险选择，不默认将全部速算、动画、导航测试用于无关改动。
- 键盘、手势、动画、导航或崩溃问题若源码检查无法证明修复，可运行对应的最小交互测试；在开始前简要说明它验证什么。
- 大范围改造可扩大回归范围，但必须说明受影响范围；不能仅因为已有测试就反复运行全部测试。
- 不添加只检查实现字符串存在、却无法验证用户行为的测试来制造“已验证”的结论。
- 检查通过后，只有新修改、明确失败或尚未解决的疑点才扩大或重复测试。
- 当前工作流若仍强制执行无关测试，明确指出约定与配置不一致。文档不会自动改变 GitHub Actions；不能声称仅写规则就已优化实际构建。

## 发布与重试

- 将一批修改和本地检查完成后再触发一次发布。推送前先核对触发条件，禁止对同一提交同时自动触发和手动触发重复构建。
- 记录本次提交 SHA、版本、workflow run ID；后续只跟踪对应运行，不重复搜索或猜测版本状态。
- 失败时先读取失败步骤及相关日志，分清应用错误、测试断言失败、基础设施异常和超时，再决定处理方式。
- 不进行无新证据的循环重试。优先复用同一提交的有效产物、重试失败任务；确实无法复用时说明完整重建原因。
- 不通过忽略真实失败来发布。超时调整必须有日志依据，不能把延长超时当作性能优化。
- 不默认下载 IPA 到本机，交付固定 Release 页面：https://github.com/baoger07122/kaogong-review/releases 。

## 等待、日志和额度

- 等待构建交给脚本或受支持的等待机制；避免每十几秒唤醒模型查询同一状态。
- 状态查询通常间隔至少 60 秒，随等待时间退避；模型主要处理阶段变化、失败、需要决策和发布完成。仍需遵守当前会话的进度更新要求。
- 一次获取日志后保留并筛选使用；只返回错误及必要上下文，不反复下载整份日志，不输出整份源码或全部工具说明。
- 优先使用已认证的 GitHub 接口。遇到限流应停止密集查询并退避，不通过换接口继续高频探测。
- 没有任务级用量数据时，不编造 token 消耗比例，也不把 GitHub 编译分钟数当作 Codex 额度消耗。

本约定适用于后续任务；用户当前的明确指令优先。
