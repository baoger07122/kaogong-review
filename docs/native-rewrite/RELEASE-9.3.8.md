# 9.3.8 / Build 31 — 首位数字动画与正确反馈修正

- 取消正确答案触发的整页绿色背景闪光，保留顶部正误小提示和手动确认后立即换题。
- 答案动画在文字、下划线和视图尺寸就绪后启动；缩放和透明度子动画均显式指定 150ms，与网页 0.8→1、0.4→1、ease-out 对齐。
- 布局使用 bounds/center，不再写入动画容器的 frame；首位输入、清空后重新输入、下一题首位输入使用同一机制，连续输入替换上一段动画而不排队。
- 保留仅答案区域缩放、整行按宽度居中、减少动态效果开关、单次按键事件和 C 删一位/图标清空。
- 增加 UIKit 实际动画层测试：检查首位数字的呈现层缩放及结束状态、首次零尺寸布局、下一题和快速连续输入；与既有模拟器点击测试一起执行。测试结果不等同于真机像素级验收。
