# 9.4.4 / Build 36 — 全局返回按钮轻量化

- iPadOS 26 下隐藏原生页面返回按钮的系统共享玻璃背景，取消显眼的胶囊框。
- 首页功能页、学习库详情与编辑页、设置子页面及速算页面统一采用静态返回按钮样式，按下时不再缩放、变色或播放突出动画。
- 保留上一版扩展的隐形点击范围，不缩小实际可点击区域。
- 返回按钮继续位于系统 `topBarLeading` 工具栏区域，保留 iPadOS 对分屏窗口控件的自动避让。

真机重点验收：返回图标是否保持简洁静态，同时仍能稳定弹出退出确认。
