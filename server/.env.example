# 复制为 .env 并修改（不要提交真实 .env 到公开仓库）
PORT=3000

# 改成一段足够长的随机字符串，例如：openssl rand -hex 32
JWT_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_STRING

# 允许访问的前端源（你的静态站地址），多个用逗号分隔；本地调试可用 *
# 例：https://your-user.github.io/kaogong-review
CORS_ORIGIN=*

# SQLite 数据库文件路径（默认 ./data.sqlite）
DB_PATH=./data.sqlite
